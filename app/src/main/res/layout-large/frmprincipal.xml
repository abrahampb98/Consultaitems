<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"> <!-- Fondo aplicado -->

    <!-- Contenido principal -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        > <!-- Padding para evitar que los elementos queden pegados a los bordes -->

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="1286dp"
            android:layout_gravity="center"
            android:gravity="center"
            android:orientation="vertical"> <!-- Alineación centrada del contenido -->

            <!-- Texto de Bienvenida -->
            <TextView
                android:id="@+id/lblUsuario"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:layout_marginTop="8dp"
                android:text="Bienvenido:"
                android:textColor="@android:color/black"
                android:textSize="18sp"
                android:textStyle="bold" /> <!-- Color de texto ajustado -->

            <!-- Imagen centrada -->
            <LinearLayout
                android:id="@+id/imagen"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="40dp"
                android:gravity="center_horizontal"
                android:orientation="vertical">

                <ImageView
                    android:id="@+id/imagen_central"
                    android:layout_width="225dp"
                    android:layout_height="268dp"
                    android:layout_gravity="center"
                    android:src="@mipmap/ic_logo_cotzul_foreground" />
            </LinearLayout>

            <!-- Texto de Versión -->
            <TextView
                android:id="@+id/txtVersion"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="bottom|center"
                android:text="version"
                android:textColor="@android:color/black"
                android:textSize="30sp" /> <!-- Color de texto ajustado -->

        </LinearLayout>

    </FrameLayout>
</androidx.drawerlayout.widget.DrawerLayout>
